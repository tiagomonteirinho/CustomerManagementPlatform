@model AppCalisto.Data.Entities.Product
@{
    ViewData["Title"] = "Details";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.10.25/css/dataTables.bootstrap4.min.css" />

<h2>Product #@Model.Id.ToString("D4")</h2>
<hr />

<div>
    <dl class="row">
        <dt class="col-sm-2">Number</dt>
        <dd class="col-sm-10">@Model.Id.ToString("D4")</dd>
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>
        <dt class="col-sm-2">Base Price</dt>
        <dd class="col-sm-10">@Model.BasePrice€</dd>
        <dt class="col-sm-2">Tax Rate</dt>
        <dd class="col-sm-10">@Model.TaxRate%</dd>
        <dt class="col-sm-2">Tax Amount</dt>
        <dd class="col-sm-10">+@Model.TaxAmount.ToString("0.00")€</dd>
        <dt class="col-sm-2">Unit Price</dt>
        <dd class="col-sm-10">@Model.UnitPrice.ToString("0.00")€</dd>
    </dl>
</div>
<div class="form-group">
    <a asp-action="Edit" asp-route-Id="@Model.Id" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-secondary">Back to list</a>
</div>
